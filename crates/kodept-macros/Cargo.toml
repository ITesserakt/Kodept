[package]
name = "kodept-macros"
version = "0.1.2"
edition = "2021"

[lints]
workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = []
size-of = ["dep:size-of"]

[dependencies]
kodept-ast = { path = "../kodept-ast", version = "0.2" }
kodept-core = { path = "../kodept-core", version = "0.2" }

codespan = "0.11.1"
codespan-reporting = "0.11.1"
derive_more.workspace = true
size-of = { optional = true, workspace = true }
tracing = "0.1.37"
itertools.workspace = true
petgraph = "0.6.4"
