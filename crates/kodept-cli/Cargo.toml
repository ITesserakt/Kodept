[package]
name = "kodept-cli"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive"] }

[lints]
workspace = true

[features]
default = []
parallel = []
