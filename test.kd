module Prim {
    enum struct Bool { True, False }

    struct Person(name: String, address: String, phones: Array) {
    	fun print_person(p: Person) {
            if Bool => var x = p.name
            else => ()
    	}
    }
}
