module Prelude =>

enum struct Bool { True, False }

struct BoolLike {
    fun xor(self: Bool, other: Bool) {}

    fun not(trait: Bool) => xor(trait, True)
}

struct Int

fun main => println(not(True))