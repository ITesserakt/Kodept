module Prelude =>

fun const(x) => \y => x

fun compose(f, g) => \x => g(f(x))

fun curry(f, a, b) => f (a, b)

fun duplicate(x) => (x, x)

fun id(x) => x

trait Num {
    abstract fun plus(self: Int, other: Int): Int
}

struct Int

fun main {
}