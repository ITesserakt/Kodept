module Testing {
    fun compose(f, g) => \x => f(g(x))

    fun complex {
        fun inner(a, b) => a(b)

        val result = inner(\x => x, 1)
        result
    }

    enum struct Bool { True, False }
}

module Testing2 {
    struct Int
}
